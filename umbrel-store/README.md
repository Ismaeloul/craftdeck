# ⚠️ Copia congelada — NO es la fuente de verdad

Esta carpeta es una copia antigua (v0.1.6) de los manifiestos de Umbrel y **no se usa para nada**.

La app real vive en el repo del store: [`Ismaeloul/umbrel-app-store`](https://github.com/Ismaeloul/umbrel-app-store) → carpeta `ismaeloul-craftdeck/`. Cualquier cambio de despliegue se hace **allí** (ver «Flujo de release» en el README raíz de este repo).

Se puede borrar esta carpeta sin miedo.
